name: nlp_course
channels:
  - defaults
dependencies:
  # pip install pip
  # pip install spacy
  # pip install numpy
  # pip install keras
  # pip install matplotlib
  # pip install pandas
  # pip install nltk
  # pip install scikit-learn
  # pip install jupyter
  - pip=18.1
  - spacy=2.0.16
  - numpy=1.15.4
  - keras
  - matplotlib=3.0.1
  - pandas=0.23.4
  - nltk=3.3.0
  - scikit-learn=0.20.1
  - jupyter=1.0.0


